# Backend (FastAPI)
fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
email-validator==2.3.0
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.5.0

# Excel / Word / PDF engines
openpyxl==3.1.5
python-docx==1.2.0
docxtpl==0.20.2
pandas==3.0.5
pypdf==6.15.0
# PDF conversion additionally requires the LibreOffice `soffice` binary
# (specifically the libreoffice-writer package) to be installed on the host.

# Frontend (Streamlit POC)
streamlit==1.61.1
requests==2.34.2

# Testing
pytest==9.1.1
